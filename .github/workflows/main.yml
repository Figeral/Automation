name: first workflow automation
on:
  push:
    branches: ["master"]

  commit_comment:
    branches: ["master"]

  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: greeting workflow
        run: 
           echo "hello world"
